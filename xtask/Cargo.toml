[package]
name = "xtask"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
# Remove the current nih_plug_xtask line and replace it with this:
nih_plug_xtask = { git = "https://github.com/robbert-vdh/nih-plug.git" }