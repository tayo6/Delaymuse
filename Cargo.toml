[lib]
name = "delaymuse" # must match what you expect in target/release/delaymuse.dll
crate-type = ["cdylib"]

[dependencies]
nih_plug = "0.32"
nih_plug_egui = { version = "0.32", features = ["widget"] }
# don't call eframe::run_native inside lib.rs, use nih_plug_egui for the editor