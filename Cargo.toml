[package]
name = "delaymuse"
version = "0.1.0"
edition = "2021"

[lib]
name = "delaymuse"
crate-type = ["cdylib"]

[workspace]
members = ["xtask"]
resolver = "2"

[dependencies]
nih_plug = "0.32.0"
nih_plug_egui = "0.32.0"

# add your other deps below if you need them, but no git = for nih_plug
